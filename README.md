# land solana transaction sender
### WIP do not use in production

### setup
install openssl with quic support

``chmod +x setup.sh``

``./setup.sh``

``./set.sh``

install land core:

``cd core && cargo b -r``
